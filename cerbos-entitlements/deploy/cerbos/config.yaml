server:
  httpListenAddr: ":3592"
  grpcListenAddr: ":3593"

storage:
  driver: disk
  disk:
    directory: /plans
    watchForChanges: true

audit:
  enabled: true
  backend: local
  local:
    storagePath: /var/lib/cerbos/audit
